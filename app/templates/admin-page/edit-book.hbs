<AddForm @heading="Edit Book" as |handleSubmit|>
    <FormComponent @btn-value="Edit Book" @id="add-form" @handleSubmit={{handleSubmit}}>
      <BookForm @book={{@model}}>
        <input type="hidden" value="{{@model.id}}">
      </BookForm>
    </FormComponent>
</AddForm>